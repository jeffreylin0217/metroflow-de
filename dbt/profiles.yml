metroflow:
  target: local
  outputs:
    local:
      type: duckdb
      path: "{{ env_var('METROFLOW_DB', '../data/warehouse.duckdb') }}"
      threads: 2
      settings:
        memory_limit: 3GB
        preserve_insertion_order: false
        enable_progress_bar: false
